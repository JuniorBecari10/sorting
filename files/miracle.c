#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>

#include "../utils.h"

void miracle_sort(int32_t *arr, size_t *len) {
    while (!sorted(arr, *len)); // maybe one day it gets sorted
}
